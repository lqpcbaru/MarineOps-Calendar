# ── Dependencies ──────────────────────────────
node_modules/

# ── Build output ──────────────────────────────
dist/
build/
.next/
*.tsbuildinfo

# ── Environment secrets ───────────────────────
.env
.env.local
.env.*.local

# ── IDE / editor ──────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Testing / coverage ────────────────────────
coverage/
.nyc_output/

# ── Docker / infra artifacts ──────────────────
docker-compose.override.yml
*.log

# ── Prisma ────────────────────────────────────
.prisma/
# prisma/*.db   # Uncomment if using SQLite for dev


.env
.env.local
.env.*.local
# Database dumps produced by infrastructure/scripts/db-backup.sh.
# These contain production data and must never be committed.
backups/
*.dump
*.dump.part

# Operator-supplied provider codes: external data, never committed.
# example.json is the template and is tracked.
infrastructure/provider-mappings/*.json
!infrastructure/provider-mappings/example.json
